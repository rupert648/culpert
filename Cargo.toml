[workspace]
resolver = "2"
members = [
    "culpert",
    "culpert-foundations",
    "culpert-tracing",
    "culpert-macros",
    "culpert-cli",
    "examples/mock-axum",
    "examples/foundations",
    "examples/tracing",
    "examples/macros",
    "spike",
]

[workspace.package]
version = "0.0.1"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/rupert648/culpert"

[workspace.dependencies]
# Internal crates
culpert = { path = "culpert", version = "0.0.1" }
culpert-foundations = { path = "culpert-foundations", version = "0.0.1" }
culpert-tracing = { path = "culpert-tracing", version = "0.0.1" }
culpert-macros = { path = "culpert-macros", version = "0.0.1" }

# culpert core deps
backtrace = "0.3"
fastrand = "2.4.1"
parking_lot = "0.12"
smallvec = "1"
# pprof export (Phase 2)
flate2 = "1"
prost = "0.14"

# foundations: pin lean feature set; full `telemetry` pulls jemalloc/sentry/etc.
# `tracing` gives us TelemetryContext, span_fn, WithTelemetryContext.
# `telemetry-server` gives us TelemetryServerRoute for the pprof endpoint.
foundations = { version = "5", default-features = false, features = ["tracing", "telemetry-server"] }

tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync", "net", "signal"] }

# mock-axum example deps
axum = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# culpert-cli deps
clap = { version = "4", features = ["derive"] }

# benches
criterion = { version = "0.7", features = ["html_reports"] }

# culpert-tracing adapter deps
tracing = "0.1"
tracing-core = "0.1"
tracing-subscriber = "0.3"

# culpert-macros proc-macro deps
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
